
<div class="col-lg-12 col-md-12">
    <form class="dash__address-make">
        <div>

            <a asp-action="Create" class="btn btn--e-brand-b-2" type="submit">Create new user</a>
        </div><br />
        <div class="dash__box dash__box--shadow dash__box--bg-white dash__box--radius u-s-m-b-30">
            <h2 class="dash__h2 u-s-p-xy-20">List Product</h2>
            <div class="dash__table-2-wrap gl-scroll">
                <table class="dash__table-2">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>User Name</th>
                            <th>Email</th>
                            <th>Roles</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <th>@item.User.Id</th>
                                <td>@item.User.UserName</td>
                                <td>@item.User.Email</td>
                                <td>@item.RoleName</td>
                                <td>
                                    <a asp-action="Edit" asp-controller="User" asp-route-Id="@item.User.Id">Edit</a>
                                    <a asp-action="Delete" asp-controller="User" asp-route-Id="@item.User.Id">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </form>
</div>

